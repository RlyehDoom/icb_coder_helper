# RoslynIndexer Configuration
# Copy this file to .env and customize your settings
# cp .env.example .env

# ============================================
# Solution Configuration
# ============================================

# Default solution to analyze
# Leave empty to auto-discover from Grafo/Repo/Cloned/
# Example: DEFAULT_SOLUTION_PATH="../Repo/Cloned/ICB7C/Infocorp.Banking.sln"
DEFAULT_SOLUTION_PATH=""

# ============================================
# Output Configuration
# ============================================

# Base output directory (used when USE_REPO_NAME_IN_OUTPUT=false)
DEFAULT_OUTPUT_DIR="./output"

# Use repository name + "_GraphFiles" as output directory
# Example: If analyzing ICB7C repo -> output goes to "./ICB7C_GraphFiles/"
USE_REPO_NAME_IN_OUTPUT="true"

# Output format (json or xml)
DEFAULT_OUTPUT_FORMAT="json"

# ============================================
# Generation Options
# ============================================

# Generate architectural graphs
GENERATE_GRAPHS="true"

# Generate CSV statistics
GENERATE_STATISTICS="true"

# Enable verbose output
VERBOSE_MODE="false"

# ============================================
# Filtering Options
# ============================================

# Symbol types to include (comma-separated)
# Options: Class, Interface, Method, Property, Field, Enum, Struct, Delegate, Event
FILTER_SYMBOL_TYPES="Class,Interface,Method,Property"

# Regex pattern to exclude projects (uses | as separator)
# Example: ".*\.Tests?$|.*\.UnitTests?$" excludes test projects
EXCLUDE_PROJECTS_REGEX=".*\.Tests?$|.*\.UnitTests?$"

# ============================================
# Usage Examples
# ============================================

# 1. Auto-discover and analyze (recommended):
#    Just run: ./build.sh analyze
#    - Will search in ../Repo/Cloned/ for repositories
#    - Prompts to select repository and solution
#    - Output goes to ./{REPO_NAME}_GraphFiles/

# 2. Specific solution with auto-discovery:
#    DEFAULT_SOLUTION_PATH="../Repo/Cloned/ICB7C/Infocorp.Banking.sln"
#    USE_REPO_NAME_IN_OUTPUT="true"
#    Output: ./ICB7C_GraphFiles/

# 3. Custom output directory:
#    DEFAULT_SOLUTION_PATH="../Repo/Cloned/MyRepo/Solution.sln"
#    USE_REPO_NAME_IN_OUTPUT="false"
#    DEFAULT_OUTPUT_DIR="./my-custom-output"
#    Output: ./my-custom-output/

# 4. Filter specific types only:
#    FILTER_SYMBOL_TYPES="Class,Interface"
#    GENERATE_STATISTICS="true"