# ========================================
# VARIABLES DE ENTORNO - PRODUCCION
# ========================================
# Copiar este archivo a .env.prod y completar con valores reales
# NO COMMITEAR .env.prod - Contiene credenciales
#
# Uso:
#   cp .env.prod.example .env.prod
#   # Editar .env.prod con credenciales reales
#   docker compose -f docker-compose.prod.yml --env-file .env.prod up -d

# ========================================
# MongoDB - PRODUCCION
# ========================================
# Formato: mongodb://USER:PASSWORD@HOST:PORT/DATABASE?authSource=admin&tls=true&tlsInsecure=true
MONGODB_CONNECTION_STRING=mongodb://your_user:your_password@your_host:28101/GraphDB?authSource=admin&tls=true&tlsInsecure=true
MONGODB_DATABASE=GraphDB
MONGODB_PROJECTS_COLLECTION=projects

# TLS Configuration
MONGODB_TLS_CERT_FILE=/app/certs/client.pem
MONGODB_TLS_INSECURE=true

# ========================================
# Query Service
# ========================================
SERVER_HOST=0.0.0.0
SERVER_PORT=8081
LOG_LEVEL=INFO
CORS_ORIGINS=*

# ========================================
# MCP Server
# ========================================
PYTHONUNBUFFERED=1
PYTHONPATH=/app

# ========================================
# Environment
# ========================================
ENVIRONMENT=production

# ========================================
# Graph Version Filtering (FALLBACK)
# ========================================
# RECOMENDADO: Cada cliente especifica version en su mcp.json URL
#              Ejemplo: "url": "http://server:8083/sse?version=7.10.3"
# Esta variable es SOLO fallback cuando el cliente no especifica ?version=
# Dejar vacio para consultar todas las versiones (cuando cliente no especifica)
GRAFO_DEFAULT_VERSION=

# ========================================
# Deployment
# ========================================
COMPOSE_PROJECT_NAME=grafo-prod
COMPOSE_FILE=docker-compose.prod.yml
